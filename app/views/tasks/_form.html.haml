= simple_form_for(@task) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.association :project
    = f.input :completed_at, as: :string

  .form-actions
    = f.button :submit
